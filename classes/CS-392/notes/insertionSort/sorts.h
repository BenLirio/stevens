#ifndef SORTS_H_
#define SORTS_H_

/* Function prototypes */
void display_array(int *array, const int length);
void bubble_sort(int *array, const int length);
void selection_sort(void *array, const int length);
void insertion_sort(int array[], const int length);

#endif
